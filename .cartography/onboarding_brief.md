# FDE Day-One Onboarding Brief

**Q1:** The primary data ingestion path is through the `load/loaders.py` module, which handles ingestion from CSV, JSON, and shapefile sources. It writes data to PostgreSQL/PostGIS and is invoked by `load/__main__.py` and tested by `tests/load/test_loaders.py`. Key evidence: [`load/__main__.py:11`]
- `load/loaders.py` is the central ingestion module with 5 commits in the last 30 days [load/loaders.py]. [`load/loaders.py:1`]
- It processes sources like `4_seeds/logement_2020_valeurs.csv` and writes to PostGIS [load/loaders.py:60, 148]. [`load/loaders.py:60`]

**Q2:** The 3-5 most critical output datasets/endpoints are: [`load/loaders.py:1`]
1. **PostGIS tables** populated by `load/loaders.py` (e.g., from sources like `cog_poste`, `logement_2020`). [`load/loaders.py:1`]
2. **CSV files** generated by `load/loaders.py` for intermediate storage . [`load/loaders.py:1`]
3. **Documentation** in `utils/generer_doc_recenssement.py` derived from `4_seeds/logement_2020_valeurs.csv` [utils/generer_doc_recenssement.py]. [`utils/generer_doc_recenssement.py:14`]
4. **Test database** accessed via `tests/fixtures/db.py` for validation [tests/fixtures/db.py]. [`tests/fixtures/db.py:1`]
Evidence: `load/loaders.py` writes to PostGIS and generates CSVs, while `utils/generer_doc_recenssement.py` produces structured docs from CSVs. [`load/loaders.py:1`]

**Q3:** If `load/loaders.py` changes its interface, the blast radius includes: [`load/loaders.py:1`]
- **Direct users**: `load/__main__.py` (main execution script), `tests/load/test_loaders.py` (tests), and `load/__init__.py` (configures loaders). [`load/__main__.py:11`]
- **Downstream**: Any processes relying on PostGIS tables or CSV outputs generated by `load/loaders.py`. [`load/loaders.py:1`]
- **Potential impact**: Broken ingestion pipeline, test failures, and disrupted data flow for dependent systems. [`load/loaders.py:1`]

**Q4:** Business logic is **concentrated** in `load/loaders.py` (ingestion workflows) and `utils/generer_doc_recenssement.py` (demographic documentation). It is **distributed** in: [`load/loaders.py:1`]
- **Database layer**: CTEs and joins (e.g., `cog_poste → postes_communes`) [data lineage]. [`make-open-data/1_data/intermediaires/geographie/postes_communes.sql:1`]
- **Configuration**: `load/__init__.py` and `tests/fixtures/db.py` handle environment-specific settings. [`load/__init__.py:1`]
Evidence: `load/loaders.py` handles core ingestion, while `utils/generer_doc_recenssement.py` focuses on transformation-specific logic. [`load/loaders.py:1`]

**Q5:** Most frequent changes (last 30 days): [`load/loaders.py:1`]
1. `load/loaders.py` (5 commits) — core ingestion logic. [`load/loaders.py:1`]
2. `load/__main__.py` (3 commits) — execution scripts. [`load/__main__.py:1`]
3. `tests/load/test_loaders.py` (3 commits) — test cases for ingestion. [`tests/load/test_loaders.py:1`]
4. `load\loaders.py` (5 commits) — same as above (likely Windows/Unix path variation). [`load/loaders.py:1`]
Evidence: Git velocity data shows `load/loaders.py` has the highest commit count. [`load/loaders.py:1`]
