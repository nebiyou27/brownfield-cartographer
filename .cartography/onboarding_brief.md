# FDE Day-One Onboarding Brief

Q1: The primary data ingestion path is managed by the `load/loaders.py` module, which handles data loading from CSV, JSON, and shapefiles into PostgreSQL databases. This module is heavily utilized across various data pipelines, with frequent commits indicating active development and maintenance. The ingestion process involves multiple steps, including reading data, executing SQL operations, and converting data formats, as evidenced by the following code locations:

- Reading CSV data: `load/loaders.py:70` [?]
- Executing SQL operations: `load/loaders.py:99`, `load/loaders.py:105`, `load/loaders.py:112` [?]
- Converting data to CSV: `<dynamic>:to_csv:load/loaders.py:60` [?]
- Converting data to PostGIS: `<dynamic>:to_postgis:load/loaders.py:148` [?]

The high commit activity in `load/loaders.py` (5 commits in the last 30 days) further confirms its central role in data ingestion. [`load/loaders.py:1`]

--- [`make-open-data\dbt_project.yml:1`]

Q2: The 3-5 most critical output datasets/endpoints are the PostgreSQL tables generated by the data ingestion process, primarily through the `load/loaders.py` module. These tables are used in downstream processes and reports, such as:

1. **dvf_2024_dev** and **dvf_2024**: Ingested via `load/loaders.py:99`, these tables store property transaction data.
2. **ventes_immobilieres**: Generated by SQL queries from multiple `dvf_` tables (e.g., `dvf_2024_dev`, `dvf_2024`, etc.), which are then used in further processing.
3. **logement_2020** and **logement_2020_dev**: These tables are used in renaming operations for recensement data, as seen in the lineage. [`make-open-data\dbt_project.yml:1`]

These datasets are critical because they serve as inputs for downstream SQL transformations and reporting, such as `preparer_dvf_biens_immobilier.sql` and `renommer_colonnes_valeurs_logement.sql`. [`make-open-data\dbt_project.yml:1`]

--- [`make-open-data\dbt_project.yml:1`]

Q3: If the most critical module (`load/loaders.py`) changes its interface, the blast radius would be extensive. This module is the central hub for data ingestion, handling CSV, JSON, and shapefile operations, and is referenced across multiple files, including:

- `load/__main__.py` (imports `load/loaders.py` for execution) [`load/loaders.py:1`]
- `tests/load/test_loaders.py` (tests the ingestion logic) [`load/loaders.py:1`]
- `utils/generer_doc_recenssement.py` (indirectly depends on data loaded by `load/loaders.py`) [`load/loaders.py:1`]

Changes to this module could disrupt data ingestion, transformation, and downstream processes, including PostgreSQL table creation and SQL-based transformations. The high commit count (5 changes in the last 30 days) indicates frequent updates, increasing the risk of breaking changes. [`make-open-data\dbt_project.yml:1`]

--- [`make-open-data\dbt_project.yml:1`]

Q4: Business logic is concentrated in the `load/loaders.py` module, which handles data ingestion, transformation, and PostgreSQL interactions. However, some logic is distributed across SQL scripts and utility modules:

- **Concentrated**: `load/loaders.py` contains core ingestion logic (e.g., CSV/JSON reading, SQL execution).
- **Distributed**: SQL scripts like `preparer_dvf_biens_immobilier.sql` and `renommer_colonnes_valeurs_logement.sql` handle transformations, while `utils/generer_doc_recenssement.py` generates documentation from CSV data.

The balance suggests that while ingestion is centralized, specific transformations and documentation generation are decentralized. [`make-open-data\dbt_project.yml:1`]

--- [`make-open-data\dbt_project.yml:1`]

Q5: The files with the most frequent changes in the last 30 days are ranked as follows: [`make-open-data\dbt_project.yml:1`]

1. **load/loaders.py** (5 commits) - Core ingestion module. [`load/loaders.py:1`]
2. **tests/load/test_loaders.py** (3 commits) - Tests for ingestion logic. [`load/loaders.py:1`]
3. **load/__main__.py** (3 commits) - Execution layer for data loading. [`load/__main__.py:1`]
4. **load/loaders.py** (3 commits) - Cross-platform ingestion module (Windows/Linux). [`load/loaders.py:1`]
5. **extract/__init__.py** (2 commits) - Data extraction module. [`extract/__init__.py:1`]

These changes reflect active development in data ingestion, testing, and cross-platform support. [`make-open-data\dbt_project.yml:1`]
